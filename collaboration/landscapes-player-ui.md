# Landscapes and player UI — 2026-09-11 (UTC)

Human contribution: accepted and locked the five construction assets; specified three immutable terrain modes, map_en HUD layout, GitHub player avatars/progress and info.png project cards. Asked how town scale can be generated from organizer input. Original instructions are in [0020](../prompts/0020-landscapes-and-player-ui.md) and [0021](../prompts/0021-automatic-town-generation.md).

AI work: inspected map_en/map_cn/info references; researched public README/license metadata for THREE.Terrain, MapGenerator, WFC and Three.js; documented alternatives and limits in [the generation assessment](../docs/procedural-town-plan.md). Implemented original terrain assembly with fixed plot transforms, flat building pads, concrete/gravel roads and cloud districts. Building GLBs are preserved by SHA-256 checks; no asset pack or new library was imported. The Three.js addons remain MIT. Existing original GLBs remain CC0. Human reference artwork is not relicensed.

Data and account implementation: manifests preserve a landscape field and reject changes for the same town; default missing values to flat. New collections use stable spiral assignments. Added a public-profile player OAuth flow while restricting publication to the configured deployer. Exploration stores viewed project IDs locally and, for signed-in players on the deployed town, in private account-scoped files. Tokens never enter public town exports. Configuration and storage requirements are in [deployment notes](../docs/landscape-deployment.md).

Verification so far: TypeScript passed. Existing eight data/API tests passed, plus three new landscape/player tests for field preservation, immutability, level pads, player/deployer separation, deduplication, account isolation and disk persistence across API restarts. Real OAuth credentials were not configured or tested. Targeted Chrome checks passed all three landscapes, card previews, progress reload, language switching, mobile layout and landscape configuration export. Codex inspected actual screenshots and corrected reversed road normals and cloud/base intersections. Final human visual acceptance remains pending.

Limits: this iteration generates different initial town sizes and preserves positions across metric snapshots. The existing contract still rejects changing a published collection's membership; incremental collection expansion requires a separate versioned migration. The terrain is an original procedural prototype, not a claim of matching every detail of the reference illustration. No walking/interior simulation, currency or payment system is introduced.
