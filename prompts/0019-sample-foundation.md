# Include the foundation in the sample town

Original instruction: “实例小镇里少了基建 第二阶段展示”

Translation: “The sample town is missing the infrastructure/foundation, the second-stage display.”

Finding: the stage-2 model exists, but no project in the default latest sample snapshot has a foundation score.

Implementation scope and acceptance: add one clearly fictional sample project with cumulative metrics that remain in the foundation interval under the unchanged default rule. Keep the existing eight projects, plots and records unchanged. Synchronize the generated sample and distributed JSON, verify all five appearances in the latest snapshot, and inspect the browser rendering. No new asset, dependency or real repository observation is introduced.
